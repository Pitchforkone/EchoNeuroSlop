# План прототипирования

## Фазы разработки

Прототип разбит на 5 фаз. Каждая фаза завершается **играбельным билдом**.

Задачи разделены:
- 💻 **Код** — реализует Copilot (C#, шейдеры)
- 🎛️ **Инспектор** — настраивает разработчик вручную в Unity Editor
- 📦 **Пакеты** — установка через Package Manager

---

## Фаза 1: Сетевая инфраструктура + FPS-контроллер

**Цель:** два игрока подключаются друг к другу и ходят по темноте.

### 📦 Пакеты (разработчик)

- [x] **1.0** Установить `com.unity.netcode.gameobjects` через Package Manager (Add by name)
- [ ] **1.0b** Установить `com.unity.multiplayer.tools` (опционально, для дебага сети)

### 💻 Код (Copilot)

- [x] **1.1** `GameNetworkManager.cs` — NetworkManager wrapper: Host/Join по IP, спавн игроков
- [x] **1.2** `PlayerController.cs` (NetworkBehaviour) — CharacterController, обзор мышью, WASD, бег (Shift), приседание (Ctrl)
- [x] **1.3** `ConnectionUI.cs` — минимальный UI: поле IP-адреса, кнопки Host / Join

### 🎛️ Инспектор (разработчик)

- [ ] **1.4** Сцена: удалить Directional Light, Ambient = чёрный, Skybox = None
- [ ] **1.5** Построить тестовый уровень из примитивов (стены, пол, коридоры, комнаты)
- [ ] **1.6** Создать Player Prefab:
  - GameObject с CharacterController + Camera (child)
  - Добавить `PlayerController`, `NetworkObject`, `NetworkTransform` (AuthorityMode = Owner)
  - Назначить в NetworkManager → Player Prefab
- [ ] **1.7** Разместить 2 спавн-точки (пустые GameObject'ы)
- [ ] **1.8** URP Asset: Depth Texture = On

### Результат фазы
Два игрока по LAN подключаются, оба ходят в темноте. Видят только чёрный экран, но управление и сеть работают.

---

## Фаза 2: Эхолокация — ядро (сетевая) + контурная визуализация

**Цель:** эхо-пульс виден обоим игрокам. Клик — волна от одного, видят оба. В зоне эхо-пульса подсвечиваются **контуры и рёбра** геометрии (edge detection), а не только заливка светом.

**Визуальный подход:** Point Light (мягкая подсветка) + fullscreen Edge Detection через URP Renderer Feature. Контуры рисуются только в зоне активных эхо-пульсов.

### 💻 Код (Copilot)

- [x] **2.1** `EchoManager.cs` (NetworkBehaviour, синглтон на сцене):
  - Массив эхо-источников (max 16), обновление, очистка
  - `SpawnEchoServerRpc(pos, intensity, color)` → `SpawnEchoClientRpc()`
  - Визуализация: Point Light спавн/анимация/удаление
- [x] **2.2** `EchoPreset.cs` (ScriptableObject) — конфигурация параметров эхо
- [x] **2.3** `PlayerEchoLocator.cs` (NetworkBehaviour):
  - Активное эхо (ЛКМ) → `ServerRpc` → видят оба игрока
  - Пассивное эхо (шаги) → `ServerRpc` (слабое, малый радиус)
  - Cooldown между эхо-событиями
- [x] **2.4** `EchoEdgeDetection.shader` — HLSL fullscreen шейдер:
  - Читает `_CameraDepthTexture` + `_CameraNormalsTexture` → Sobel/Roberts edge detection
  - Принимает массив эхо-источников (позиции, радиусы, цвета, fade) через global shader properties
  - Контуры рисуются только в зоне активных пульсов, за пределами — чёрный
  - Цвет контуров берётся из эхо-пресета, интенсивность затухает вместе с пульсом
- [x] **2.5** `EchoEdgeFeature.cs` + `EchoEdgePass` — URP ScriptableRendererFeature:
  - Рендерит fullscreen quad с `EchoEdgeDetection.shader`
  - Инжектится после рендеринга (BeforeRenderingPostProcessing)
  - Запрашивает DepthNormals текстуру
- [x] **2.6** Обновлен `EchoManager.cs` — передача данных активных эхо-пульсов в шейдер:
  - `Shader.SetGlobalVectorArray` / `Shader.SetGlobalFloatArray` каждый кадр
  - Каждый пульс: position, currentRadius, color, fade

### 🎛️ Инспектор (разработчик)

- [ ] **2.7** Добавить `EchoManager` (+ NetworkObject) на сцену
- [ ] **2.8** Добавить `PlayerEchoLocator` на Player Prefab
- [ ] **2.9** Создать EchoPreset ассеты: «ActivePing» (20м, яркий), «Footstep» (5м, слабый)
- [ ] **2.10** Назначить EchoPreset ассеты в PlayerEchoLocator
- [ ] **2.11** Добавить `EchoEdgeFeature` в PC_Renderer
- [ ] **2.12** PC_Renderer: Depth Texture = On, DepthNormals = On (если не включено)

### Результат фазы
Игрок 1 кликает → оба видят расходящийся свет + яркие контуры геометрии в зоне пульса. Рёбра предметов обрисовываются, давая понимание формы окружения. Шаги дают слабую контурную подсветку.

---

## Фаза 3: Голос → Эхо

**Цель:** говоришь в микрофон — создаёшь эхо-волну от своего персонажа.

### 💻 Код (Copilot)

- [ ] **3.1** `MicrophoneCapture.cs` (MonoBehaviour, локальный):
  - `Microphone.Start()` → `AudioClip.GetData()` → RMS каждые ~50мс
  - Callback при превышении порога громкости
  - Работает только на owner-клиенте
- [ ] **3.2** Интеграция с `PlayerEchoLocator`:
  - MicrophoneCapture → callback → `PlayerEchoLocator.OnVoiceDetected(intensity)`
  - → `SendEchoServerRpc(position, intensity)` → оба видят эхо
  - Маппинг громкости → радиус/интенсивность: тихий шёпот = 3м, нормальная речь = 10м, крик = 20м
- [ ] **3.3** `MicrophoneSettings.cs` (ScriptableObject) — порог, cooldown, маппинг громкости

### 🎛️ Инспектор (разработчик)

- [ ] **3.4** Добавить `MicrophoneCapture` на Player Prefab
- [ ] **3.5** Создать MicrophoneSettings ассет и настроить пороги
- [ ] **3.6** Протестировать: говорить в микрофон → должно появляться эхо

### Результат фазы
Игрок говорит → от его персонажа расходится эхо-волна. Громче говоришь = больше видно. Оба игрока видят эхо друг друга.

---

## Фаза 4: Враг + звуковая экосистема

**Цель:** враг охотится на звук, мир «живёт» своими звуками.

### 💻 Код (Copilot)

- [ ] **4.1** `EnemyAI.cs` (NetworkBehaviour, server-authoritative):
  - NavMeshAgent, запускается только на host (`if (!IsServer) return`)
  - Состояния: Patrol → Investigate → Chase → Search → Patrol
  - Слушает эхо-события от EchoManager: `OnSoundHeard(position, intensity)`
  - Сам генерирует эхо при движении (красное, ServerRpc → ClientRpc)
  - `NetworkTransform` для синхронизации позиции
- [ ] **4.2** `AmbientEchoSource.cs` (MonoBehaviour, не сетевой):
  - Циклический источник: капли, вентиляция
  - Одинаковый на обоих клиентах (детерминированный таймер)
- [ ] **4.3** `EchoAudioBridge.cs` — helper: воспроизвести звук + создать эхо синхронно

### 🎛️ Инспектор (разработчик)

- [ ] **4.4** Создать Enemy Prefab: NavMeshAgent + EnemyAI + NetworkObject + NetworkTransform
- [ ] **4.5** Назначить Enemy Prefab для спавна на host при старте
- [ ] **4.6** Запечь NavMesh (Window → AI → Navigation → Bake)
- [ ] **4.7** Расставить точки патруля врага
- [ ] **4.8** Расставить ambient-источники: капающая вода (голубой), вентиляция (серый)
- [ ] **4.9** Создать EchoPreset ассеты: вода (голубой, слабый), враг (красный, средний)
- [ ] **4.10** Добавить звуковые файлы в Audio/

### Результат фазы
Враг патрулирует, слышит игроков, идёт на звук. Своё движение враг визуализирует красным эхо. Ambient-источники мерцают.

---

## Фаза 5: Геймплейная петля

**Цель:** полный игровой цикл — цель, стратегия, win/lose.

### 💻 Код (Copilot)

- [ ] **5.1** `ThrowableObject.cs` (NetworkBehaviour) — подобрать и бросить камень:
  - Создаёт эхо в точке падения → отвлекает врага
  - Спавн/бросок синхронизированы через ServerRpc
- [ ] **5.2** `WinZone.cs` (NetworkBehaviour) — триггер на выходе, оба игрока должны дойти
- [ ] **5.3** `LoseCondition.cs` — враг поймал игрока → game over для обоих
- [ ] **5.4** `GameStateManager.cs` (NetworkBehaviour) — состояния: Playing → Won / Lost
- [ ] **5.5** Минимальный HUD: кулдаун эхо, состояние напарника (жив/пойман)

### 🎛️ Инспектор (разработчик)

- [ ] **5.6** Спроектировать уровень-лабиринт для двоих (достаточно большой для тактик)
- [ ] **5.7** Разместить WinZone (триггер на выходе)
- [ ] **5.8** Разместить камни-ThrowableObject по уровню
- [ ] **5.9** Настроить врага: скорость, дальность слуха, агрессивность
- [ ] **5.10** Перепечь NavMesh для финального уровня

### Результат фазы
Два игрока в лабиринте. Говорят в микрофон чтобы видеть. Враг идёт на звук. Камни для отвлечения. Нужно добраться до выхода вдвоём.

---

## Фаза 2.5 (опционально): Апгрейд визуала — расходящееся кольцо

**Цель:** добавить видимый фронт расходящейся волны (ring) к существующим контурам.

### 💻 Код (Copilot)

- [ ] **2.5.1** Обновить `EchoEdgeDetection.shader` — добавить визуализацию кольца (ring) фронта волны
- [ ] **2.5.2** Опционально: убрать Point Light, полностью перейти на screen-space визуализацию

### 🎛️ Инспектор (разработчик)

- [ ] **2.5.3** Настроить параметры кольца (ширина, яркость) через Material/Feature
- [ ] **2.5.4** Решить: оставить Point Light для мягкой заливки или только контуры+кольцо

---

## Приоритеты

| Приоритет | Что | Почему |
|---|---|---|
| 🔴 P0 | Фаза 1 (сеть + контроллер) | Без сети нет кооператива |
| 🔴 P0 | Фаза 2 (сетевое эхо) | Core-механика |
| 🔴 P0 | Фаза 3 (голос → эхо) | USP — голос как источник видимости |
| 🟡 P1 | Фаза 4 (враг + ambient) | Создаёт хоррор и tension |
| 🟢 P2 | Фаза 5 (геймплей) | Полный цикл |
| 🟢 P2 | Фаза 2.5 (визуал) | Polish, когда геймплей подтверждён |

## Метрики успеха прототипа

1. ✅ Два игрока подключаются и **видят эхо друг друга**
2. ✅ Голос в микрофон **создаёт эхо-волну** пропорциональную громкости
3. ✅ Игроки чувствуют **дилемму** «говорить = видеть + опасность»
4. ✅ Враг **реагирует на звуки** и создаёт ощущение угрозы
5. ✅ Стабильные **60 FPS** с 2 игроками и 8+ эхо-источниками
6. ✅ Уровень **проходим** при кооперации двух игроков
